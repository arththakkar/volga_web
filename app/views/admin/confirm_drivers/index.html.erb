<section class="content-header">
  <h1>
    Confirm Drivers
    <small>List</small>
  </h1>
  <ol class="breadcrumb">
    <li><a href="<%= admin_root_path %>"><i class="fa fa-dashboard"></i> Dashboard</a></li>
    <li class="active">Confirm Drivers</li>
  </ol>
</section>

<section class="content">
  <div class="row">
    <div class="col-md-12">
      <div class="box">
        <div class="box-header">
          <h3 class="box-title">List of Confirm Drivers</h3>
          <div class="box-tools">
            <%= link_to "<i class='fa fa-plus'></i>".html_safe, new_admin_confirm_driver_path, class: "btn btn-info btn-primary" %>
          </div>
        </div>
      	<div class="box-body">
          <table id="example1" class="table table-bordered table-striped">
            <thead>
              <tr>
                <th>Name</th>
                <th>Mobile Number</th>
                <th>Car</th>
                <th>Car Model</th>
                <th>Plan</th>
                <th>Area</th>
                <% if current_user.is_admin? %>
                  <th>Added By</th>
                <% end %>
              </tr>
            </thead>
            <div id="f_list">
              <tbody>
                <% @confirm_drivers.each do |driver| %>
                  <%= puts driver.id %>
                  <tr>
                    <td><%= link_to driver.full_name, edit_admin_confirm_driver_path(driver) %></td>
                    <td><%= driver.mobile_number %></td>
                    <td><%= driver.car.name %></td>
                    <td><%= driver.car_model %></td>
                    <td><%= driver.scheme %></td>
                    <td><%= driver.area.name %></td>
                    <% if current_user.is_admin? %>
                      <td><%= driver.confirm_franchise.try(:name) %></td>
                    <% end %>
                  </tr>
                <% end %>
              </tbody>
            </div>
          </table>
        </div>
      </div>
    </div>
  </div>
</section>
<script type="text/javascript">
	$('#example1').dataTable();
</script>